# Open Questions — taleb-pi Execution Plan

## taleb-pi-execution-plan (revision 3.2 — ACCEPTED) — 2026-05-05

Items deferred to user decision or runtime smoke test resolution. Each links back to a plan section + the review that surfaced it.

### Iter-4 Architect / Critic new issues (NI-rev4-1 through NI-rev4-3) — STATUS at rev 3.2

The rev 3.1 plan triggered 3 NI-rev4 polish items (all SHOULD FIX, all LOW severity, all surgical) from iter-4 reviews. Rev 3.2 absorbs them via lightweight doc pass (no full P→A→C re-cycle, mirroring rev 2.5 pattern).

- [x] **NI-rev4-1 — P1.0 ambiguous-outcome decision tree missing fallback rule** — **ADDRESSED in rev 3.2** (plan line 577). Added "Ambiguous outcome" branch between Outcomes B-2 and C: defaults to Outcome C (halt the plan) when 1-2 of the 5 probe markers fire, OR partial dispatch trace, OR contradictory signals. Documents AND-condition for Outcome A explicitly. Records cost-asymmetry rationale (false-halt ~30 min vs false-proceed ~1 day rework). P1.0 hard-gate sentence updated (line ~588) to reference the new ambiguous-default.
- [x] **NI-rev4-2 — P1.0 `task ` grep matcher loose (trailing-space false-positive risk)** — **ADDRESSED in rev 3.2** (plan lines 555–569). Tightened the matcher from loose `task ` (matches LLM prose containing the word "task") to a union of structural tool-dispatch forms: `<tool_use[^>]*task` (XML form), `"name":\s*"task"` (JSON event form), `task --` (CLI form), `task(` (invocation form). Added Step 3b: explicit executor manual visual inspection step that supplements the grep. Inline comment documents why the loose matcher was insufficient (LLM prose like "I'll dispatch a task to MELCHIOR-1..." would false-positive).
- [x] **NI-rev4-3 — 1537-line plan needs onboarding note for executor** — **ADDRESSED in rev 3.2** (plan lines 3–24, before revision history). New "How to read this document" section names Required reading (~400 lines: rev history + §1 Principles + §6 P1–P5 + §8 ADR) vs Reference during execution (~1100 lines: §2–§5, §7, register-exemplar.md, open-questions.md). Explicitly identifies P1.0 as the entry point and HARD GATE. Final plan length post-3.2 edits: 1583 lines (delta +46 from rev 3.1's 1537; the onboarding section is the largest single addition).

**Net resolution status of rev 3.1 NI-rev4 items:** all 3 ADDRESSED in rev 3.2 via mechanical polish edits (~9 minutes total work as Critic predicted). No architectural changes; no additional MUST FIX/SHOULD FIX surfaced. ADR Status flipped from "ITERATING (rev 3.1 pending review)" to **"ACCEPTED (revision 3.2)"**. Plan is final; executor handoff begins at P1.0.

### Iter-4 Critic open question (Q-iter4-1) — calibration item, post-acceptance

- [ ] **Q-iter4-1 — Pass 2 acceptance bar tautology check** (Critic CO-iter4-3) — The default-mode 8-question coherence bar (≥7/8 produce sensible Taleb-flavored response) may be too generous in isolation. Cross-test value (Pass 1 disagreement × Pass 2 coherence diagnoses different failure modes) gives it real signal, but the single-bar absolute level is uncalibrated. **Action deferred to post-Pass-2-results audit at P4 acceptance**: if 8/8 questions trivially pass with implicit Taleb application (no explicit Taleb concept naming), tighten the bar to require explicit naming on at least 4/8 questions to differentiate Taleb-flavored from Taleb-prompt-with-no-real-effect. Logged for empirical resolution; does not block executor handoff.

### Iter-3 Critic / Architect new issues (NI-rev3-1 through NI-rev3-7) — STATUS at rev 3.1

The rev 3.0 plan introduced 7 NIs from the iter-3 reviews. Rev 3.1 absorbs them as follows:

- [x] **NI-rev3-1 — Slash-routing pre-flight (HIGH; blocks P1)** — **ADDRESSED in rev 3.1**. Plan now has P1.0 hard gate at top of P1 (rev 3.1 §6 P1.0); 5-minute test against user's actual `omp` binary with three branches (Outcome A / B-1 / B-2 / C). Outcome A = continue rev 3.0 plan; Outcome B-1 = rewrite all `/magi` → `/skill:magi-tribunal`; Outcome B-2 = preserve `commands/` and create `commands/magi.md` thin wrapper; Outcome C = halt entire plan. Documented at `.omc/qa/slash-routing-result.md` (created at P1.0). Plan §1 Principle 4 updated to reflect the unverified-mechanism honest framing.
- [x] **NI-rev3-2 — Bilingual trigger keyword surface conflates two mechanisms** — **ADDRESSED in rev 3.1**. §1.D split into 1.D.1 (slash-command resolution at parse-time, depends on P1.0 outcome) and 1.D.2 (in-context regex at turn-generation-time, lives in alwaysApply rule bodies). Architectural caveat added: regex inside non-alwaysApply skill bodies is dead code at the in-context level.
- [x] **NI-rev3-3 — Per-MAGI Taleb cueing identical with role swap-slot** — **ADDRESSED in rev 3.1**. P2 amendment block at §6 (was line ~467) replaced. Three distinct opening paragraphs for MELCHIOR/BALTHASAR/CASPER citing different Taleb works (Black Swan / Antifragile Ch 7 / Antifragile Ch 18-19). Per-agent open paragraphs now reflect different relationships to Taleb (falsifier / via-negativa-guardian / convexity-seeker). PM6 acceptance (Jaccard ≤0.45) is more achievable.
- [x] **NI-rev3-4 — Peacemaker geometry asserted, not re-derived** — **ADDRESSED in rev 3.1**. §1.B factually corrected — the cross-validation report (`.omc/scientist/reports/20260505_013647_magi_cross_validation.md:74`) DOES use archetype reasoning ("the conservative mother → her protective instinct → natural tie-breaker"). The rev 3.0 claim "geometry not archetype" was wrong about the source. Rev 3.1 honest framing: optimization-target geometry is a *substitute* for the archetype shortcut, not an inheritance. PM7 now has TWO E2E tests: Test A (synthesizer template structural integrity) AND Test B (M+B vs C counterfactual generation geometry — does BALTHASAR-2 read as ALLY of MELCHIOR-1?). Falsification path documented.
- [x] **NI-rev3-5 — Default-mode capability under-specified** — **ADDRESSED in rev 3.1**. New §1.C.1 explicit two-tier capability disclosure. Default mode delivers (in order): 8 alwaysApply rules + 2 P3 new (ergodicity, positive-convexity) + 1 cherry-picked (cognitive-state-diagnosis) + 1 magi-protocol-hint = ~10 rules; SYSTEM.md persona; 6 skills on-demand; `/magi` recommendation hint. Default mode does NOT deliver: tribunal verdicts, structured 3-0/2-1/1-1-1 votes, per-turn debate diversity. ≥90% of user interactions go through default mode (estimated). Principle 2 reframed honestly: "MAGI tension is opt-in via `/magi`; when invoked, mandatory; default mode has no tension surface."
- [x] **NI-rev3-6 — 50-token always-apply hint is the wrong tool for PM8** — **ADDRESSED in rev 3.1**. O8 in §3 expanded with Option C (TTSR-triggered injection via `rules/magi-suggest-trigger.md` with `condition:` regex). Option A remains primary (rev 3.1 unchanged choice); Option C is documented but NOT YET BUILT. Pivot trigger: PM8 telemetry shows `/magi`-rate <5% over 30 days AND sample size ≥20 high-stakes-marker turns (R19 caveat). Pivot cost: ~1 hour. R19 added to risk table.
- [x] **NI-rev3-7 — Register check regex is necessary but insufficient** — **ADDRESSED in rev 3.1**. New `.omc/plans/register-exemplar.md` (created in rev 3.1; HARD GATE before P2.1). Three exemplars from current Chinese register markers (file:line cited): imperative + em-dash + product/process framing; adversarial discomfort framing; permission-reversal pattern. Each exemplar has a direct/flat translation (failure mode), Taleb-register-preserving translation (target), hedge-regex pass/fail per version, adversarial-bite test, acceptance regex per file. New `.omc/qa/scripts/check-positive-register.sh` greps for exemplar acceptance patterns; exits non-zero if file has zero matches where exemplar applies.

**Net resolution status of rev 3.0 NIs:** all 7 ADDRESSED in rev 3.1; 5 MUST FIX absorbed surgically; 3 SHOULD FIX absorbed (with Critic CO-1/CO-2/CO-3 confirmed already-resolved-by-careful-reading; Critic CO-4 absorbed as ADR Negative; Critic CO-5 noted as OPTIONAL and deferred to executor judgment).

### Iter-2 Critic / Architect new issues (NI-1, NI-2, NI-3) — ADDRESSED in rev 2.5 (carried forward in rev 3.0/3.1)

- [x] **NI-1 — Smoke-test sequencing contradiction** (was: P2 acceptance probe required P5.3 to be complete) — **ADDRESSED** in rev 2.5. Plan now creates a 5-line stub `skills/incerto-search/SKILL.md` at P2 acceptance time (P2.7); the probe at P2 acceptance verifies the **skill-access mechanic** using the stub; P5.4-pre re-runs the probe with real data after P5.3 ships the real skill body. P5.3 explicitly **replaces** the stub rather than creating fresh, preserving file-path/`name` continuity.
- [x] **NI-2 — Parallelism timestamp false-pass** (was: dispatch-spread check passed when each child's first action is fast under serial dispatch) — **ADDRESSED** in rev 2.5. P2 acceptance criterion replaced with ratio test: `max(end_ts) − min(start_ts) ≤ 1.5 × max(individual_duration)`.
- [x] **NI-3 — Cross-file label drift** (was: per-file unit checks passed even when `保守派` appeared in one file and `保守者` in another) — **ADDRESSED** in rev 2.5. Cross-file consistency grep added to atomicity step. Rev 3.0 updated for new English non-canon labels (`Empirical Skeptic|Tail-Risk Guardian|Convexity Seeker`); rev 3.1 unchanged.

### Architectural unknowns (resolve at runtime via smoke tests)

- [ ] **Slash-routing mechanism** (Plan §6 P1.0; rev 3.1 NEW) — Empirical pre-flight required before P1 deletes `commands/`. Outcomes A/B-1/B-2/C documented. **The pre-flight must be run on the user's actual `omp` binary, not in sandbox/CI. The `omp` verb names (`--list-commands`, `--list-skills`, etc.) must be verified via `omp --help` first (Q-iter3-1 carryover).**
- [ ] **Subagent skill access** (Plan §6 P5.4-pre, R11) — Does oh-my-pi propagate `skill://` access to subagents spawned via `task`? Probe runs **twice**: at P2 acceptance with the stub (mechanic-only test), at P5.4-pre with the real skill body (real-data test). Outcomes written to `.omc/qa/skill-access-result.md` (Pass A / Pass B / Fail).
- [ ] **Parent-emission parallelism on `task` calls** (Plan §6 P2 acceptance ratio test, R12, PM4) — Does writing 3 `task` blocks in one assistant message actually fire them simultaneously, or does the runtime serialize? P2 ratio test resolves; pass = `max(end_ts) − min(start_ts) ≤ 1.5 × max(individual_duration)`, fail = ratio > 2.0; document in `.omc/issues/p2-parallelism-collapse.md`.

### Spec adjudications recorded but reversible

- [ ] **`narrative-fallacy-guard.md` TTSR rule** (Plan §1.A) — Plan honors spec line 35 ("no TTSR for narrative fallacy; MAGI handles it") over spec line 419 (Round 14 transcript proposed creating the TTSR rule). If user disagrees with this adjudication, flag before P2 ships.
- [ ] **BM25 vs vector retrieval** (Plan §3 O3, ADR Consequences > Negative) — Plan picks BM25 despite research's #1-ranked LanceDB+Transformers.js. User explicitly overrode the research in Round 16. Migration trigger: recall@5 < 60% on Chinese subset for 3 consecutive battery runs → flip to LanceDB; ~1-2 day swap.
- [ ] **MAGI canon labels dropped** (rev 3.0/3.1 amendment 1.B; user decision Round 17 post-rev-2.5) — Spec lines 40, 76-107, 184-202 overridden. Honestly named in §1.B; logged in ADR Consequences > Negative. **Rev 3.1 SF1 correction: optimization-target geometry is a substitute for the archetype shortcut, not an inheritance — empirical verification at PM7 Test B (counterfactual M+B vs C). If falsified, pivot path documented.**
- [ ] **MAGI fires every response → MAGI fires only on `/magi`** (rev 3.0/3.1 amendment 1.C; user decision post-rev-2.5) — Spec lines 38, 372, 399 overridden. Honestly named in §1.C and §1.C.1. Default mode = no MAGI tension surface; ≥90% of user interactions estimated to go through default mode.
- [ ] **English rewrite of system prompts** (rev 3.0/3.1 amendment 1.D; spec is silent on prompt language) — User decision post-rev-2.5. ~17 files translated. Bilingual trigger keyword lists preserved at the points where Chinese user input must match. **Rev 3.1 MF5 absorption: `.omc/plans/register-exemplar.md` is the calibration anchor; PM5 hedge regex + new positive register check (`check-positive-register.sh`) are the two-layer safety net.**

### Calibration items (resolve empirically post-launch)

**Resolved by rev 3.0/3.1 amendment (deletion):**
- [x] **Pre-classification gate threshold** (was: "≤80 chars + greeting markers" for State A bypass; "≤200 chars + no decision verbs" for State B MELCHIOR-only) — **RESOLVED by rev 3.0 amendment 1.C**. The 3-state graduated gate collapses to 1-state (default = no MAGI; `/magi` = full MAGI). State A and State B no longer apply; calibration item is invalidated, not pending.
- [x] **MAGI cost telemetry interpretation** (was: "if cost exceeds expected envelope, revisit graduated gate's State B threshold") — **RESOLVED by rev 3.0 amendment 1.C**. Metric is now `/magi`-rate (does the user invoke MAGI rarely or often?), not graduated-gate cost. Different metric, different interpretation. Tracked under PM8 telemetry follow-up; R19 (rev 3.1) adds sample-size caveat.

**Active calibration items:**
- [ ] **DeepSeek-v4-flash adequacy for English MAGI prompts** (Plan §7 R13, ADR Consequences > Neutral) — No empirical test specified; P2 bail-out catches if the model can't anchor irreconcilable values. **Rev 3.0 changed character of this concern**: rev 2.5 was Chinese baseline; rev 3.0/3.1 is English baseline (D4 expectation: more reliable, but unverified). If P2 fires bail-out at <4/8, model swap is one of the diagnostics.
- [ ] **Stage 4's 75% projection vs measured rate** (Plan §6 P2 bail-out) — 75% target was a Stage 4 simulation under canon-labeled, default-fire architecture. Rev 3.0/3.1 changes both: non-canon labels AND opt-in. **Rev 3.1 ADR Negative explicitly acknowledges this (per Critic CO-4): the 75% projection does NOT transfer; P2 bail-out is the only validation; `[P2-WARN]` ship at 4/8-5/8 is more probable under rev 3.1 than rev 2.5.**
- [ ] **Crystallization threshold N=10** (Plan §6 P5.5, ADR Follow-ups) — Default is a magic number; adjustable via `.omc/incerto/config.json`. Tune after first 100 query log entries reveal demand distribution.
- [ ] **`bm25` npm package health** (Plan §6 P5.2) — `npm view bm25` pre-flight check at P5.2 start; if package is deprecated/yanked, swap to `wink-bm25-text-search` (option B from O3) and document in commit + ADR.
- [ ] **PM6 Jaccard threshold (≤0.45)** (rev 3.1 carryover; Q-iter3-5) — Asserted but not calibrated. **Rev 3.1 MF4 (differentiated per-MAGI cueing) should make 0.45 more achievable; threshold itself remains uncalibrated.** After P4 ships, the user should consider running PM6 audit on rev 2.5's Chinese prompts for comparison — establishes a baseline before committing to the rev 3.0/3.1 threshold.
- [ ] **PM8 acceptance bar (≥3/5 high-stakes inputs surface suggestion)** (rev 3.1 carryover; Architect NI-rev3-6) — 60% bar is unjustified by data. Telemetry pivot path documented (Option C TTSR rule); R19 sample-size caveat applies.

### Documentation / housekeeping

- [ ] **Concept-stub hand-curation** (Plan §6 P5.1) — Ingest script outputs `concept-stub.json` with empty `concepts: []` per chapter. User fills in as a separate post-P5 pass; PM2 mitigation depends on this.
- [ ] **Synonyms.json layer** (Plan §6 P5.5 follow-up) — Build after first 100 query log entries surface Chinese-vs-English mismatch patterns.
- [ ] **MAGI cost telemetry** (ADR Follow-ups; rev 3.1 updated) — Aggregate `query-log.jsonl` weekly; R19 sample-size caveat: pivot only if rate <5% over 30 days AND sample size ≥20 high-stakes-marker turns; below 20, extend window to 60 days.
- [ ] **Pandoc availability documentation** (Plan §6 P5.1) — If pandoc fallback (`epub2`) is consistently used on the user's Windows-WSL environment, document a Windows pandoc install path in `.omc/incerto/README.md`.
- [ ] **Battery question refinement** (ADR Follow-ups) — Round-1 timestamp + vote pattern data may reveal the 8-question battery is itself biased; revise question selection if disagreement saturates.
- [ ] **Research artifact header note** (rev 3.0/3.1 follow-up) — Add a one-line note to `.omc/scientist/reports/20260505_013647_magi_cross_validation.md` and `.omc/research/magi-tension-design/report.md` pointing to rev 3.1 plan §1.B canon-label change. **The artifacts themselves are not rewritten.**
- [ ] **PM6 escape valve evaluation** (rev 3.0/3.1 follow-up) — 3 months post-deploy, audit MAGI Round 1 outputs across 10+ `/magi` invocations; if pairwise Jaccard regularly >0.45, drop Taleb cue from BALTHASAR-2 and CASPER-3 prompts. Document in ADR consequences.
- [ ] **PM7 falsification path** (rev 3.1 NEW per SF1) — If PM7 Test B (M+B vs C counterfactual) shows BALTHASAR-2 reading as ALLY of MELCHIOR-1 in 3/3 cases, pivot to either (a) re-introduce canon-language partial annotation or (b) accept BALTHASAR-2 as ally-of-M and update synthesizer templates. Decision deferred to empirical PM7 Test B result at P4 acceptance.
- [ ] **Open question for Architect/Critic review (carried over from rev 3.0)**: should PM8 mitigation include a soft auto-fire — i.e., when the recommendation-trigger fires, the main agent offers `/magi` *and* shows what the first MAGI's verdict would look like as a teaser? Marked open. Rev 3.1 documents Option C TTSR rule as the mechanical pivot path; soft auto-fire is a different question (UX, not mechanism).

### Iter-3 Architect open questions absorbed into the plan

- [x] **PM7 counterfactual M+B vs C test** (Architect SHOULD FIX #7) — **ADDRESSED in rev 3.1 SF1**. PM7 now has Test A (existing) + Test B (counterfactual). Falsification trigger documented; recovery paths (canon partial-annotation OR ally-not-mediator template rewrite) named.
- [x] **PM8 mechanism upgrade to TTSR** (Architect SHOULD FIX #8 / NI-rev3-6) — **DOCUMENTED-AS-PIVOT in rev 3.1 MF2**. Option A remains primary; Option C (TTSR rule) is the documented pivot path. Critic SHOULD FIX rating accepted (not promoted to MUST FIX) because PM8 telemetry catches failure with bounded loss.

### Iter-3 Critic open questions (Q-iter3-1 through Q-iter3-6) — STATUS at rev 3.1

- [ ] **Q-iter3-1 — `omp` verb names** — P1.0 pre-flight starts with `omp --help` to map verb names. If `--list-commands` is wrong verb name, executor adjusts pre-flight before running. Rev 3.1 plan body for P1.0 explicitly includes this step.
- [ ] **Q-iter3-2 — PM6 escape valve asymmetric cueing alternative** (drop Taleb from MELCHIOR-1 instead of BALTHASAR-2/CASPER-3) — Marked open; user judgment required if PM6 fires.
- [ ] **Q-iter3-3 — In-context regex evaluation at parse-time vs turn-generation-time** — Rev 3.1 §1.D.2 names this explicitly: regex inside alwaysApply rule bodies is matched by the LLM during turn generation (not by the runtime at parse time). Empirical verification deferred — if the LLM doesn't reliably attend to the regex during generation, PM8 mitigation underperforms; pivot to Option C TTSR rule (which uses runtime parse-time regex matching).
- [ ] **Q-iter3-4 — Cross-validation report partial annotation depth** — Rev 3.1 plan honors user's preference (one-line header note pointing to rev 3.1 plan §1.B). If user wants stronger annotation, that's a 30-minute documentation pass; not a finding.
- [ ] **Q-iter3-5 — PM6 Jaccard threshold calibration** — Marked under "active calibration items" above.
- [ ] **Q-iter3-6 — `omp --X` verification commands** — Subset of Q-iter3-1; resolved at P1.0 verb-name mapping step.

---

*Open questions tracker updated 2026-05-05 for rev 3.2 (ACCEPTED). Net rev 3.1 NI-rev4 status: 3/3 ADDRESSED (lightweight doc pass). Net rev 3.0 NI status: 7/7 ADDRESSED. Net rev 2.5 NI status: 3/3 ADDRESSED (carried forward). Active calibration items: 6 (+1 new Q-iter4-1 deferred to post-Pass-2 audit). Architectural unknowns: 3. Spec adjudications: 5 (none silently ignored). Final ADR Status: ACCEPTED. Executor handoff begins at P1.0.*
